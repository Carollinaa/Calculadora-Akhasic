#Faça um Programa que peça o raio de um círculo, calcule e mostre sua área

raio = float(input("Didite o raio do circulo""\n"))

area = 3.14 * (raio ** 2)
print("are do seu círculo é: ", area)


#r = float(input('Raio do círculo: '))
#Aqui, o programa solicita ao usuário que insira o raio do círculo. O input() captura essa entrada como uma string inicialmente.
#float() converte essa string para um número de ponto flutuante (float), pois o raio pode ser um número decimal.
#Cálculo da área:

#area = 3.14 * (r ** 2)
#Calcula a área do círculo usando a fórmula matemática: área = π * raio².
#O valor 3.14 é uma aproximação do valor de π (pi) usado neste exemplo. Em cálculos mais precisos, pode-se utilizar math.pi para obter o valor mais preciso de π disponível no Python.
#Saída de dados:

#print('Área do círculo: ', area)
#Exibe a mensagem "Área do círculo: " seguida pelo valor calculado da área.
#area contém o valor calculado anteriormente através da fórmula.